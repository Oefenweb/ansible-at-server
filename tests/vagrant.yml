# test file for at-server
---
- hosts: all
  remote_user: vagrant
  become: true
  pre_tasks:
    - include: pre.yml
  roles:
    - ../../
  vars:
    at_server_authorized_keys:
      - src: "{{ playbook_dir }}/../files/id_rsa.pub"
